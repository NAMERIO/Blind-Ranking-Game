{
  "name": "project2",
  "version": "1.0.0",
  "scripts": {
    "server": "node src/server.js",
    "client": "live-server public --no-browser",
    "dev": "concurrently \"pnpm run server\" \"pnpm run client\"",
    "start": "pnpm run dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sharp": "^0.32.1"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "live-server": "^1.2.1"
  }
}
